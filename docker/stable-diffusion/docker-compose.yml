services:
  stable-diffusion:
    image: johnguan/stable-diffusion-webui
    container_name: kassandra-stable-diffusion
    ports:
      - "7861:7860"
    volumes:
      - sd-models-stable-diffusion:/data/models/Stable-diffusion
      - sd-models-lora:/data/models/Lora
      #- sd-outputs:/app/stable-diffusion-webui/outputs
    environment:
      # CLI arguments for the WebUI
      # Using SD 3 Medium model for better prompt adherence
      #- COMMANDLINE_ARGS=--api --xformers --medvram --no-half-vae --listen --port 8080 --skip-torch-cuda-test
      #- COMMANDLINE_ARGS=--api --xformers --no-half --no-half-vae --listen --port 8080 --skip-torch-cuda-test
      - COMMANDLINE_ARGS=--api --xformers --listen --port 7860
      # Set default model to SD 3 Medium
      - SD_MODEL=protovisionXLHighFidelity3D_beta0520Bakedvae.safetensors
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [ gpu ]
    restart: unless-stopped

volumes:
  sd-models-stable-diffusion:
    external: true
    name: sd-models-stable-diffusion
  sd-models-lora:
    external: true
    name: sd-models-lora
